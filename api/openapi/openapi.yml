openapi: 3.0.3

info:
  title: Expo Management API
  description: REST API for Expo application
  version: 1.0.0

servers:
  - url: http://localhost:8080/api
    description: Development server

paths:
  /version:
    get:
      operationId: getVersion
      summary: Getting version
      tags:
        - Version
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetVersion"

components:
  schemas:
    GetVersion:
      type: object
      required:
        - version
      properties:
        version:
          type: string